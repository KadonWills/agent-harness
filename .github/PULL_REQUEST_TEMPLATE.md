## What

<!-- One or two sentences. The PR title must be a conventional commit header,
     e.g. "feat(ast): add go parser" — it becomes the squash commit. -->

## Why

<!-- Link the issue or RFC. Contract changes (harness.toml, manifest,
     public traits, CLI surface) require an accepted RFC. -->

Closes #

## How tested

<!-- New/updated tests, fixtures, manual steps. "No tests" needs a reason. -->

## Checklist

- [ ] `./scripts/check.sh` passes locally
- [ ] Commits are signed off (`git commit -s`, DCO)
- [ ] Tests added/updated (or explained why not)
- [ ] Docs updated if behavior or contracts changed
- [ ] If authored with an AI agent: agent disclosed above, output reviewed by me
